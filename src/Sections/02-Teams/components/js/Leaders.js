import styles from '../css/Leaders.module.scss';
import {css} from '../../../helpers';

export default function Leaders(props) {
  return (
    <div className={css(styles, 'leaders')}>
      <h1>Leaders</h1>
    </div>
  );
}
